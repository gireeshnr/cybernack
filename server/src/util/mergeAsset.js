// src/util/mergeAsset.js
export function mergeAssetData(zoomeyeData, shodanData, nmapData) {
  return {
    zoomeye: zoomeyeData,
    shodan: shodanData,
    nmap: nmapData,
    // Additional data merging logic
  };
}
